# Direct dependencies only. Python 3.13 (see README for setup).
#
# This file must stay UTF-8 without a BOM. It was previously saved as UTF-16,
# which pip cannot read inside a Linux container.
#
# GPU NOTE: PyPI ships a CPU-only torch on Windows. For CUDA, install torch
# from the PyTorch index AFTER this file, because resolving the packages below
# will otherwise pull the CPU wheel over the top of it:
#
#   pip install -r requirements.txt
#   pip install --force-reinstall torch --index-url https://download.pytorch.org/whl/cu128
#
# cu128 suits an Ampere card (RTX 30xx) on a recent driver. Pick the index
# matching your CUDA runtime from https://pytorch.org/get-started/locally/.
# Leaving torch as it installs from PyPI gives a working CPU build, which is
# what a plain container should use.

# --- Reinforcement learning ---
gymnasium==1.3.0
stable_baselines3==2.9.0
sb3_contrib==2.9.0
torch==2.11.0

# --- Numerics ---
numpy==2.5.3

# --- Persistence ---
SQLAlchemy==2.0.52
psycopg2-binary==2.9.13
python-dotenv==1.2.3

# --- Messaging ---
confluent-kafka==2.15.1

# --- Observability ---
prometheus-client==0.26.0
