[pytest]
testpaths = tests
addopts = -ra
# TensorFlow, pulled in by util/tokenizers.py, is noisy at import time and
# emits deprecation warnings from its own internals. Ticket 03 removes it.
filterwarnings =
    ignore::DeprecationWarning:tensorflow.*
    ignore::DeprecationWarning:tf_keras.*
